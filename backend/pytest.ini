[pytest]
markers =
    allow_llm: tests that need real LLM behavior (opt-out of autouse disable_llm fixture)
    asyncio: mark async tests (pytest-asyncio)
asyncio_mode = auto
filterwarnings =
    ignore::DeprecationWarning
    ignore::PendingDeprecationWarning